import 'package:flutter/material.dart';

class AppColors {
  // Fondo principal del juego
  static const Color bgColor = Color(0xFF0F0B21);

  // Color de las cards / contenedores
  static const Color cardColor = Color(0xFF1A1730);

  // Color acento principal (morado vibrante)
  static const Color accentColor = Color(0xFF6E4BFF);

  // Texto morado suave
  static const Color textPurple = Color(0xFF9E8FFF);

  // Colores adicionales útiles para el juego
  static const Color impostorRed = Color(0xFFFF3B3B);
  static const Color citizenGreen = Color(0xFF00E676);
  static const Color warningOrange = Color(0xFFFFC107);

  // Colores de botones
  static const Color buttonPrimary = Color(0xFF6E4BFF);
  static const Color buttonSecondary = Color(0xFF3D2CFF);

  // Colores neutros útiles
  static const Color whiteOpacity05 = Colors.white10;
  static const Color whiteOpacity08 = Colors.white12;
  static const Color whiteOpacity10 = Colors.white10;
}
